import { CheckoutClient } from '@/components/store/CheckoutClient';

export const metadata = {
  title: 'Checkout — Il Mio Negozio',
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
